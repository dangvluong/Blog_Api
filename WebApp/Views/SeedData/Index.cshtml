<div class="container">
    @if (TempData["message"] != null)
    {
        <div class="alert alert-info">@TempData["message"]</div>
    }

    <a asp-action="ClearData" class="btn btn-danger">Clear Data</a>
    <a asp-action="SeedData" class="btn btn-primary">Seed Data</a>
</div>